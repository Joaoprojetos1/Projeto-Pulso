{
  "name": "@pulso/fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Dados FALSOS para teste. Nenhum dado real de cliente entra aqui.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@pulso/core": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
